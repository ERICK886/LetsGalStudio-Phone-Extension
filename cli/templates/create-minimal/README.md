# {{title}}

LetsGal Studio 手机内页扩展（minimal）。`pnpm install` → `pnpm build` → 产物 `dist/index.mjs`。

- `@avg-studio/sdk`：按 Studio 文档安装（本地 sdk 或官方包）。
- `@ink-zenly/phone-sdk`：npm 版本 `{{phoneSdkVersion}}`；未发布时请手改 `package.json`。
- 宿主设置 `phoneAppId` = **`{{appId}}`**。
