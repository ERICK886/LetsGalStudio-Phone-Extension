# 自定义手机扩展使用教程

> 适用扩展：`自定义手机扩展`（`ink.zenly.ext-7a9373`）  
> 程序 UI：`phone`  
> 版本：`0.1.0`

本教程面向 LetsGal Studio 项目作者，按“安装构建 → 配置桌面 → 在剧情中挂载 → 显示聊天消息 → 交付测试”的顺序说明如何使用手机扩展。手机是**游戏运行时能力**：必须先在剧情中挂载，玩家才能用快捷键打开它，剧情也才能显示手机消息。

## 你将完成什么

完成本教程后，你的游戏将具备以下流程：

```text
进入剧情
  → 调用「挂载手机」
  → 玩家随时按 ArrowUp 打开手机桌面
  → 玩家从手机启动存档、设置或你配置的其他功能
  → 剧情调用「显示手机消息」展示角色聊天
  → 在不再需要时调用「卸载手机」
```

## 1. 安装、构建与导入

### 1.1 准备扩展产物

在扩展工作区根目录执行以下命令：

```powershell
npm install
npx tsc --noEmit
npm run build
```

构建完成后，Studio 通过 `extension.json` 中的 `entry: "dist/index.mjs"` 加载扩展。开发期间如需持续构建，可自行在终端运行：

```powershell
npm run watch
```

### 1.2 将扩展加入项目

在 LetsGal Studio 中将扩展安装或链接到你的游戏项目。确认扩展列表出现“自定义手机扩展”，然后打开其扩展设置。若 Studio 尚未加载新版本，重新构建后刷新 Preview 或按 Studio 的扩展重载流程重新加载。

> 不要修改本项目的 `sdk/` 目录；SDK 文件由 LetsGal Studio 同步维护。

## 2. 最小可运行示例

先只配置一个最小流程，验证扩展能工作，再逐步添加应用和消息：

1. 创建一个剧情 Fragment，例如“游戏开始”。
2. 添加“调用扩展方法”动作块。
3. 模块选择“自定义手机扩展”或 Studio 显示的扩展名称。
4. 方法选择“挂载手机（`mount-phone`）”。
5. 运行 Preview，进入该 Fragment 后按 `ArrowUp`。
6. 手机桌面出现后，按 `Escape` 或点击外部遮罩关闭它。

如果此步骤成功，说明扩展已正确载入且手机已在本次运行中挂载。


## 3. 挂载与卸载：手机的生命周期

手机默认处于**未挂载**状态。此状态不会写入存档：读档、重新开始或重开 Preview 后，应由剧情再次调用“挂载手机”。

### 3.1 何时挂载

通常在玩家第一次获得手机、进入可使用手机的章节，或完成新手引导后调用：

```text
调用扩展方法 → 挂载手机（mount-phone）
```

挂载只启用能力，**不会自动弹出手机**。挂载成功后：

- 玩家可按默认 `ArrowUp` 打开普通手机；
- 剧情可调用“显示手机消息”；
- 手机应用可启动其已配置的目标。

### 3.2 何时卸载

在手机被剧情收走、切换到不允许使用手机的玩法，或游戏结束前，可添加：

```text
调用扩展方法 → 卸载手机（unmount-phone）
```

卸载会立即关闭普通手机或消息手机，并结束正在等待玩家确认的消息流程。它**不会**删除玩家的背景、图标、颜色和应用动作绑定；重新调用“挂载手机”后仍会使用这些保存的偏好。

## 4. 配置普通手机桌面

在 Studio 的扩展设置中找到“自定义手机扩展”。建议按“外观 → 动作 → 应用”的顺序配置。

### 4.1 设置手机外观

| 设置项 | 用法 |
| --- | --- |
| 手机标题 | 普通手机顶部显示的标题，默认是“手机”。 |
| 手机弹出位置 | 可选择左上、中上、右上、左下、中下、右下或中部。普通手机从该方向滑入；剧情消息可单独覆盖位置。 |
| 默认背景色 | 没有背景图和 CSS 背景时使用的颜色。 |
| 默认背景图 | 在 Studio 素材选择器中选择图片。 |
| 默认 CSS 背景值 | 填写单个 `background` 值，例如 `linear-gradient(135deg, #182848, #4b6cb7)`。 |
| 默认强调色 / 默认外壳颜色 | 控制焦点、消息气泡和手机外框的颜色。 |

CSS 背景不是完整样式表：不能使用 `url()`、`@` 规则、分号或花括号。这样可以避免外部资源加载和额外 CSS 注入。

### 4.2 配置作者动作目录

动作是“点击应用后要做什么”。作者端有四个动作分组；每一行都需要稳定且唯一的 ID。推荐使用小写字母、数字和连字符，例如 `open-map`、`save-game`。

#### 动作 · 程序 UI

用于打开代码实现的程序 UI。

1. 点击“添加程序 UI 动作”。
2. 填写动作 ID、名称和可选说明。
3. 填写 UI 引用：同一扩展填 `ui-id`；其他扩展填 `extension-id/ui-id`。
4. **不要**在程序 UI 引用前添加 `@`。

#### 动作 · 可视化 UI

用于打开 Studio 制作的可视化界面。

1. 添加可视化 UI 动作。
2. 项目自己的 UI 填 `ui-name`。
3. 其他扩展提供的 UI 填 `@extension-id/ui-name`。
4. 根据需要选择是否“模态”打开；模态界面会阻止底层交互，直到关闭。

#### 动作 · 内置系统界面

用于打开 Studio 已绑定的系统槽位。可从下拉菜单选择标题画面、对话工具栏、存档、读档、设置、历史记录或鉴赏。

#### 动作 · 手机内部方法

只提供三个安全能力：

- 快速存档；
- 快速读档；
- 切换全屏。

这些命令是固定白名单，不能输入任意命令或 JavaScript。

### 4.3 添加桌面应用

动作配置完成后，在“手机应用目录”中添加应用。每一行含义如下：

| 字段 | 填写方式 |
| --- | --- |
| 应用 ID | 唯一、稳定的 kebab-case ID，例如 `settings`。 |
| 应用名称 | 玩家看到的图标名称，最多 24 个字符。 |
| 应用图标 | 可选的 Studio 图片素材；未选择时使用名称首字作为占位。 |
| 启用 | 关闭后，该应用不会显示在桌面。 |
| 锁定玩家编辑 | 开启后，玩家不能修改该应用的名称、图标和默认动作绑定。 |
| 默认动作 ID | 填写前面动作目录中已经存在的动作 ID，必须完全一致。 |

例如，创建“设置”应用的基本配置：

```text
应用 ID：settings
应用名称：设置
启用：是
锁定玩家编辑：否
默认动作 ID：settings
```

其中 `settings` 动作应在“动作 · 内置系统界面”中配置为“设置界面”。

> 未配置有效应用目录时，扩展会回退到内置的存档、读档、设置、历史、鉴赏和快捷工具应用。你添加自定义动作不会让这些默认动作消失；同 ID 的自定义动作会覆盖默认动作。

## 5. 玩家如何使用普通手机

手机打开时是四列应用网格。所有启动方式最终都会执行同一条应用动作，因此键盘与鼠标的结果一致。

| 操作 | 效果 |
| --- | --- |
| `ArrowUp` / `ArrowDown` / `ArrowLeft` / `ArrowRight` | 移动应用焦点。 |
| `Enter` / `Space` | 启动当前焦点应用。 |
| 鼠标悬停 | 把焦点移动到该图标。 |
| 鼠标点击 | 启动被点击应用。 |
| `Escape`、右上角关闭按钮、点击外部遮罩 | 关闭普通手机。 |

`ArrowUp` 只在手机关闭时用于打开手机；手机已经打开时，它只负责向上移动焦点。打开动作的默认键是 `ArrowUp`，但可以在 Studio 的输入按键配置中重映射。

## 6. 允许或限制玩家个性化

打开普通手机后，玩家可点击右上角齿轮进入“个性化手机”面板。作者可通过以下开关控制权限：

| 开关 | 关闭后的效果 |
| --- | --- |
| 允许玩家个性化手机 | 隐藏齿轮入口，并忽略玩家所有已保存的名称、图标、背景、颜色、动作覆盖和绑定。数据不删除。 |
| 允许玩家更换背景 | 禁止上传玩家背景图；作者背景仍然生效。 |
| 允许玩家更换图标 | 禁止上传玩家应用图标；作者图标仍然生效。 |

在允许个性化时，玩家可以：修改非锁定应用的名称和动作绑定、上传 PNG/JPEG/WebP 背景和图标、调整强调色/外壳色、设置安全 CSS 背景值、创建或编辑动作，以及恢复项目默认设置。

玩家偏好写入 `preferences` 的 `shared` 存档，跨普通存档槽位保留。背景图片最大 2 MB，应用图标最大 512 KB；只接受 PNG、JPEG 和 WebP。

## 7. 在剧情中显示手机聊天消息

“显示手机消息（`show-message`）”是一个会等待玩家操作的剧情方法。请先确保当前流程已调用“挂载手机”。

### 7.1 创建第一组消息

1. 在目标 Fragment 中添加“调用扩展方法”。
2. 选择“显示手机消息”。
3. 在“第 1 条 · 角色”中用角色选择器选择角色。
4. 可选：在“第 1 条 · 角色立绘”中选择该角色的立绘。
5. 填写“第 1 条 · 内容”。
6. 选择发送方：`对方发消息` 表示左侧气泡，`我方发消息` 表示右侧气泡。
7. 按需填写第 2～8 条。空内容会自动跳过；后续条目未选角色时会沿用第 1 条角色。
8. 为单独一组消息保留默认设置：`接续上一组消息 = 否`，`本组结束后关闭手机 = 是`。

第一条会立即显示。之后玩家每点击一次消息手机，或按一次 `Enter` / `Space`，都会显示下一条。最后一条显示后，需要**再次**点击或按键，手机才会关闭，剧情才继续。

### 7.2 正确选择角色立绘

角色和立绘都应使用 Studio 的选择器，不能手动填头像文件路径。头像的回退顺序是：

1. 当前消息所选的角色立绘；
2. 角色默认头像；
3. 角色的第一张立绘；
4. 角色名称首字。

若最终显示为首字，请确认该角色存在可解析的头像或立绘素材，并确认立绘确实属于当前选择的角色。

### 7.3 设置消息手机位置

每个消息块都可单独设置“手机消息显示位置”：左上、中上、右上、左下、中下、右下或中部弹出。它只影响当前消息会话的位置；普通手机位置由扩展设置中的“手机弹出位置”控制。

## 8. 超过 8 条消息时如何接续

一个“显示手机消息”方法块最多容纳 8 条。消息超过 8 条时，连续添加多个方法块，并严格按下表配置：

| 块 | 接续上一组消息 | 本组结束后关闭手机 | 说明 |
| --- | --- | --- | --- |
| 第 1 组 | 否 | 否 | 创建新的消息列表；显示完后保留手机，剧情进入下一块。 |
| 中间组 | 是 | 否 | 在旧消息下追加，显示完后继续下一块。 |
| 最后一组 | 是 | 是 | 在旧消息下追加；最后一条后再次确认，关闭手机并继续剧情。 |

两组共 10 条消息的示例：

```text
显示手机消息（第 1～8 条）
  接续上一组消息：否
  本组结束后关闭手机：否

显示手机消息（第 9～10 条）
  接续上一组消息：是
  本组结束后关闭手机：是
```

不要在第一组结束后插入“卸载手机”，也不要给第二组关闭“接续上一组消息”；否则第二组不会在第一组消息下面继续显示。

## 9. 高级：应用启动 Fragment 或扩展方法

玩家动作表单及旧配置兼容两类高级目标：

- **Fragment**：填写 `fragmentId`，若 Fragment 不在当前章节，再填写 `chapterId`；启动后由 `ctx.flow.callFragment()` 执行。
- **扩展方法**：当前 SDK 不支持代码按字符串直接调用扩展方法。应让手机先调用一个“方法适配 Fragment”，再在该 Fragment 内放置正式的“调用扩展方法”动作块。

这两类目标不在作者端动作目录的四个分组中直接显示。一般项目优先使用程序 UI、可视化 UI、系统界面和手机内部方法；只有确实需要剧情跳转或适配其他扩展时，再使用高级目标。

## 10. Preview 验收清单

在发布前，建议按以下顺序在 Studio Preview 测试：

1. 游戏运行后，未挂载时按 `ArrowUp` 不应打开手机。
2. 执行“挂载手机”后，按 `ArrowUp` 能打开普通手机。
3. 方向键可移动焦点；`Enter` / `Space` 与鼠标点击启动同一个应用。
4. `Escape`、关闭按钮和外部遮罩都能关闭普通手机。
5. 卸载手机会关闭当前 UI；重新挂载后可以再次打开。
6. 单组消息会逐条出现，最后一条后需再确认一次才关闭。
7. 两组或更多组消息能持续追加，不丢失第一组内容。
8. 角色立绘可正常显示；失效素材能回退为头像或角色首字。
9. 保存玩家个性化后，切换普通存档槽位仍能读取该设置。
10. 关闭玩家个性化权限后，齿轮入口隐藏；重新开启后，先前保存的偏好恢复生效。

## 11. 常见问题与排查

### 按 ArrowUp 没有手机

确认以下条件：

1. 当前剧情已经调用“挂载手机”；
2. 当前没有其他普通手机或消息手机正在显示；
3. Studio 输入按键配置没有把“打开手机”重映射到其他按键；
4. 扩展构建产物已刷新并被项目加载。

### 调用显示手机消息后没有界面

检查：手机是否已挂载；第 1 条是否选择了角色且填写了非空内容；是否在 Preview 游戏画面中查看而不是仅停留在编辑器面板；控制台是否有 `[phone-debug] sequence-error` 日志。

### 第二组消息没有接续

第 1 组必须是“接续上一组消息：否”“本组结束后关闭手机：否”；第 2 组起必须是“接续上一组消息：是”。最后一组才打开“本组结束后关闭手机”。

### 最后一条消息出现后剧情卡住

这是正常的确认步骤。请再点击一次游戏预览中的消息手机，或按 `Enter` / `Space`。不要在 Studio Inspector、脚本列表等编辑器区域点击，因为它们不属于游戏输入区域。

### 图片或头像没有显示

玩家上传的图片必须是 PNG、JPEG 或 WebP，且不能超过大小限制。角色头像请检查角色素材、角色立绘和当前消息选择的角色是否对应。无法加载的图片会自动使用下一个候选或角色首字。

## 12. 获取调试信息

扩展会在浏览器控制台输出 `[phone-debug]` 日志。排查消息问题时，请保留同一时间段的以下日志并一并提供：

```text
sequence-request
sequence-created
ui-show-start
listener-subscribe
sequence-publish
advance-received
sequence-error
sequence-finally
```

其中 `sequenceId` 可用于判断不同日志是否属于同一组消息。请同时说明你的 Fragment 顺序，以及每个“显示手机消息”块中“接续上一组消息”和“本组结束后关闭手机”的实际值。

---

本文档只描述当前已实现功能。应用排序、隐藏、拖拽、主题 ID、完整 CSS 编辑器等内容尚未实现；请不要依据历史方案文档把它们当作当前功能使用。